{
  "name": "cmux-remote-tooling",
  "private": true,
  "scripts": {
    "lint": "biome lint setup.js setup.test.ts launchd.test.ts client/src server/src e2e playwright.config.ts",
    "e2e": "bun run --cwd client build && playwright test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.13",
    "@playwright/test": "1.63.0"
  }
}
